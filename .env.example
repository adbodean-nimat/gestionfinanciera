# URL publica del backend. En produccion se recomienda HTTPS o una ruta relativa
# como /api servida por el mismo origen.
VITE_API_BASE_URL=/api

# Version visible en la pantalla de configuracion.
VITE_APP_VERSION=1.0.0
